global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "webhook-api"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - host.docker.internal:5073

  - job_name: "webhook-worker"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - host.docker.internal:5074

  - job_name: "rabbitmq"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - rabbitmq:15692

  - job_name: "rabbitmq-per-object"
    metrics_path: "/metrics/per-object"
    static_configs:
      - targets:
          - rabbitmq:15692
